name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4.5.3

      - name: Upload site to GitHub Pages
        uses: actions/upload-pages-artifact@v4.0.8
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1.2.9

      - name: 🚀 Deploy to GitHub Pages
        uses: actions/deploy-pages@v1.2.9

